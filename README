1) Install Meteor

  $> curl https://install.meteor.com | /bin/sh

2) Install the meteor version manager and package manager

  $> sudo npm install -g meteorite

3) Install the packages using mrt

  $> mrt

* MRT will run the server, but you can run the server manually next time:

  $> meteor